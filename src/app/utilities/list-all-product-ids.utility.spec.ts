import { listAllProductIds } from './list-all-product-ids.utility'

describe('listAllProductIds', () => {
    it('should be a function', () => {
        expect(typeof listAllProductIds).toBe('function')
    })
})
