import { calculateItemTotal } from './calculate-item-total.utility'

describe('calculateItemTotal', () => {
    it('should be a function', () => {
        expect(typeof calculateItemTotal).toBe('function')
    })
})
