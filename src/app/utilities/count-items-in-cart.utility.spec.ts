import { countItemsInCart } from './count-items-in-cart.utility'

describe('countItemsInCart', () => {
    it('should be a function', () => {
        expect(typeof countItemsInCart).toBe('function')
    })
})
