import { updateProductTally } from './update-product-tally.utility'

describe('updateProductTally', () => {
    it('should be a function', () => {
        expect(typeof updateProductTally).toBe('function')
    })
})
