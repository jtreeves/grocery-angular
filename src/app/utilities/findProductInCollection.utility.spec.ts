import { findProductInCollection } from './findProductInCollection.utility'

describe('findProductInCollection', () => {
    it('should be a function', () => {
        expect(typeof findProductInCollection).toBe('function')
    })
})
