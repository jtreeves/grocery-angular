import { filterOutEmptyItems } from './filter-out-empty-items.utility'

describe('filterOutEmptyItems', () => {
    it('should be a function', () => {
        expect(typeof filterOutEmptyItems).toBe('function')
    })
})
