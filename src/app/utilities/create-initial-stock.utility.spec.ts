import { createInitialStock } from './create-initial-stock.utility'

describe('createInitialStock', () => {
    it('should be a function', () => {
        expect(typeof createInitialStock).toBe('function')
    })
})
