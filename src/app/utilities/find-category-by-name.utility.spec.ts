import { findCategoryByName } from './find-category-by-name.utility'

describe('findCategoryByName', () => {
    it('should be a function', () => {
        expect(typeof findCategoryByName).toBe('function')
    })
})
